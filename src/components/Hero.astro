--- 
import { siteConfig } from "../config";
---
<div
  id="hero"
  class="relative isolate overflow-hidden py-24 md:h-screen"
  style={`--mouse-x: 50%; --mouse-y: 20%`}
>
  <div class="absolute inset-0 -z-10 hero-bg"></div>

  <script is:inline>
    (function initAccentAndPersistedTheme(){
      try {
        const hex = `${siteConfig.accentColor}`?.trim() || '#1d4ed8';
        const h = hex.replace('#','');
        const rgb = (h.length === 3)
          ? [parseInt(h[0]+h[0],16), parseInt(h[1]+h[1],16), parseInt(h[2]+h[2],16)]
          : [parseInt(h.slice(0,2),16), parseInt(h.slice(2,4),16), parseInt(h.slice(4,6),16)];
        document.documentElement.style.setProperty('--accent-rgb', rgb.join(','));
        document.documentElement.style.setProperty('--accent-color', `#${h}`);
        document.documentElement.style.setProperty('--hero-accent-rgb', rgb.join(','));
      } catch(e) {}
      try {
        const ls = localStorage.getItem('theme'); // 'dark' | 'light' | null
        if (ls === 'dark') document.documentElement.classList.add('dark');
        else if (ls === 'light') document.documentElement.classList.remove('dark');
      } catch(e) {}
    })();
  </script>

  <style>
    .hero-name {
      display: flex;
      flex-direction: column;
      gap: 0.25rem;
      line-height: 0.95;
      font-family: var(--name-font);
      font-weight: 600;
      font-size: clamp(2.4rem, 6.8vw, 5.5rem); /* larger than before */
      margin: 0;
      letter-spacing: -0.02em;
    }
  
    .hero-name .name-english {
      font-family: var(--heading-font), var(--name-font);
      font-weight: 600;
      font-size: inherit;
    }
  
    .name-cycle {
      display: block;
      margin-top: 0.15rem;
      font-family: var(--body-font);
      font-size: clamp(1.0rem, 2.1vw, 1.2rem);
      color: rgba(var(--muted-text-color), 1);
      height: 1.2em;
      position: relative;
      overflow: visible;
    }
  
    .name-variant {
      position: absolute;
      left: 0;
      top: 0;
      opacity: 0;
      transform: translateY(6px);
      transition: opacity .45s ease, transform .45s ease;
      white-space: nowrap;
    }
    .name-variant.show {
      opacity: 1;
      transform: translateY(0);
    }
  
    .name-variant[data-script="devanagari"] { font-family: var(--name-deva); }
    .name-variant[data-script="kannada"]   { font-family: var(--name-kannada); }
    .name-variant[data-script="tulu"]      { font-family: var(--name-tulu), var(--name-deva); }
  
    .hero-subtitle { font-weight: 400; color: rgba(var(--text-color), .9); }

    .hero-bg {
      position: relative;
      transition: background-position 120ms linear, background 180ms linear;
      background: radial-gradient(
        1000px 1400px at var(--mouse-x) var(--mouse-y),
        rgba(var(--hero-accent-rgb), var(--hero-stop-accent-1-alpha)) 0%,
        rgba(var(--hero-accent-rgb), var(--hero-stop-accent-2-alpha)) 25%,
        rgba(var(--hero-stop-mid-rgb), var(--hero-stop-mid-alpha)) 35%,
        rgba(var(--hero-end-rgb), var(--hero-end-alpha)) 100%
      );
      background-blend-mode: normal;
    }

    .hero-bg::after {
      content: "";
      position: absolute;
      inset: 0;
      pointer-events: none;
      background-image:
        linear-gradient(135deg, rgba(var(--hero-accent-rgb), 0.03) 0%, transparent 30%),
        linear-gradient(225deg, rgba(var(--accent-rgb), 0.015) 0%, transparent 40%);
      opacity: 1;
      mix-blend-mode: normal;
    }

    .hero-bg-svg { pointer-events: none; }

    .hero-content { color: rgba(var(--text-color), 1); }
    .hero-content svg { stroke: currentColor; fill: none; }
    .hero-content svg.fill-current { fill: currentColor; stroke: none; }

    .accent { color: var(--accent-color); }

    @keyframes fadeIn { from { opacity: 0; transform: translateY(20px);} to { opacity:1; transform: translateY(0);} }
    .animate-fade-in { animation: fadeIn 0.8s ease-out forwards; opacity: 0; }
    .animation-delay-200 { animation-delay: 0.2s; }
    .animation-delay-400 { animation-delay: 0.4s; }
    .animation-delay-600 { animation-delay: 0.6s; }
  </style>

  <svg
    aria-hidden="true"
    class="hero-bg-svg absolute inset-0 -z-10 size-full [mask-image:radial-gradient(100%_100%_at_top_right,white,transparent)]"
  >
    <defs>
      <pattern x="50%" y={-1} id="grid-pattern" width={200} height={200} patternUnits="userSpaceOnUse">
        <path d="M.5 200V.5H200" fill="none" style={`stroke: rgba(var(--accent-rgb,29,78,216), var(--symbols-opacity)); stroke-width: 1`} />
      </pattern>

      <pattern id="programming-symbols" x="0" y="0" width="400" height="400" patternUnits="userSpaceOnUse">
        <text x="50"  y="50"  style="fill: rgba(var(--accent-rgb,29,78,216), var(--symbol-text-alpha));" font-family="monospace" font-size="24" transform="rotate(-15)">&lt;/&gt;</text>
        <text x="150" y="100" style="fill: rgba(var(--accent-rgb,29,78,216), var(--symbol-text-alpha));" font-family="monospace" font-size="20" transform="rotate(10)">{}</text>
        <text x="250" y="80"  style="fill: rgba(var(--accent-rgb,29,78,216), var(--symbol-text-alpha));" font-family="monospace" font-size="18" transform="rotate(-5)">=&gt;</text>
        <text x="100" y="200" style="fill: rgba(var(--accent-rgb,29,78,216), var(--symbol-text-alpha));" font-family="monospace" font-size="22" transform="rotate(15)">[]</text>
        <text x="300" y="180" style="fill: rgba(var(--accent-rgb,29,78,216), var(--symbol-text-alpha));" font-family="monospace" font-size="20" transform="rotate(-10)">&lt;&gt;</text>
        <text x="200" y="250" style="fill: rgba(var(--accent-rgb,29,78,216), var(--symbol-text-alpha));" font-family="monospace" font-size="24" transform="rotate(5)">()</text>
        <text x="50"  y="320" style="fill: rgba(var(--accent-rgb,29,78,216), var(--symbol-text-alpha));" font-family="monospace" font-size="18" transform="rotate(-8)">::</text>
        <text x="350" y="300" style="fill: rgba(var(--accent-rgb,29,78,216), var(--symbol-text-alpha));" font-family="monospace" font-size="22" transform="rotate(12)">==</text>
        <text x="150" y="350" style="fill: rgba(var(--accent-rgb,29,78,216), var(--symbol-text-alpha));" font-family="monospace" font-size="20" transform="rotate(-15)">++</text>
        <text x="250" y="370" style="fill: rgba(var(--accent-rgb,29,78,216), var(--symbol-text-alpha));" font-family="monospace" font-size="24" transform="rotate(8)">;</text>
      </pattern>
    </defs>

    <rect fill="url(#programming-symbols)" width="100%" height="100%" style="opacity:var(--symbols-opacity)"></rect>
    <rect fill="url(#grid-pattern)" width="100%" height="100%" stroke-width={0} style="opacity:calc(var(--symbols-opacity) * 0.6)"></rect>
  </svg>

  <div class="h-full mx-auto p-8 sm:p-12 md:p-24 flex items-center hero-content">
    <div>
      <h2 class="text-pretty text-xl sm:text-2xl md:text-5xl font-bold tracking-tight text-current animate-fade-in">
        Hello! 👋
      </h2>

      <h1 class="mt-6 sm:mt-8 md:mt-10 hero-name animate-fade-in animation-delay-200" role="img" aria-label={`Name ${siteConfig.name}`}>
        <span class="name-english accent" data-lang="en">I'm {siteConfig.name}</span>
        <span class="name-cycle" aria-hidden="true">
          <span class="name-variant" data-script="devanagari">निष्कल् राव्</span>
          <span class="name-variant" data-script="kannada">ನಿಷ್ಕಲ್ ರಾವ್</span>
          <span class="name-variant" data-script="tulu">നിഷ്‌കൾ രാവ്</span>
        </span>
      </h1>

      <p class="mt-4 sm:mt-6 md:mt-8 hero-subtitle text-pretty text-base sm:text-lg md:text-xl/8 font-medium text-current animate-fade-in animation-delay-400">
        {siteConfig.title}
      </p>
    </div>
  </div>

  <div class="absolute bottom-0 left-0 right-0 p-8 sm:p-12 md:p-24 flex gap-x-4 sm:gap-x-6 md:gap-x-8 text-current animate-fade-in animation-delay-600">
    {siteConfig.social?.email && (
      <a href={`mailto:${siteConfig.social.email}`} aria-label="Email" class="transition-colors duration-300 hover:text-[var(--accent-color)]">
        <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="h-6 w-6 sm:h-7 sm:w-7 md:h-8 md:w-8">
          <path stroke="none" d="M0 0h24v24H0z" fill="none" />
          <path d="M3 7a2 2 0 0 1 2 -2h14a2 2 0 0 1 2 2v10a2 2 0 0 1 -2 2h-14a2 2 0 0 1 -2 -2v-10z" />
          <path d="M3 7l9 6l9 -6" />
        </svg>
      </a>
    )}
    {siteConfig.social?.github && (
      <a href={siteConfig.social.github} target="_blank" rel="noopener noreferrer" aria-label="GitHub" class="transition-colors duration-300 hover:text-[var(--accent-color)]">
        <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="h-6 w-6 sm:h-7 sm:w-7 md:h-8 md:w-8">
          <path stroke="none" d="M0 0h24v24H0z" fill="none" />
          <path d="M9 19c-4.3 1.4 -4.3 -2.5 -6 -3m12 5v-3.5c0 -1 .1 -1.4 -.5 -2c2.8 -.3 5.5 -1.4 5.5 -6a4.6 4.6 0 0 0 -1.3 -3.2a4.2 4.2 0 0 0 -.1 -3.2s-1.1 -.3 -3.5 1.3a12.3 12.3 0 0 0 -6.2 0c-2.4 -1.6 -3.5 -1.3 -3.5 -1.3a4.2 4.2 0 0 0 -.1 3.2a4.6 4.6 0 0 0 -1.3 3.2c0 4.6 2.7 5.7 5.5 6c-.6 .6 -.6 1.2 -.5 2v3.5" />
        </svg>
      </a>
    )}
    {siteConfig.social?.instagram && (
      <a href={siteConfig.social.instagram} target="_blank" rel="noopener noreferrer" aria-label="Instagram" class="transition-colors duration-300 hover:text-[var(--accent-color)]">
        <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="h-6 w-6 sm:h-7 sm:w-7 md:h-8 md:w-8">
          <rect x="4" y="4" width="16" height="16" rx="4" />
          <circle cx="12" cy="12" r="3" />
          <circle cx="16.5" cy="7.5" r="0.5" />
        </svg>
      </a>
    )}
    {siteConfig.social?.blog && (
      <a href={siteConfig.social.blog} target="_blank" rel="noopener noreferrer" aria-label="Blog" class="transition-colors duration-300 hover:text-[var(--accent-color)]">
        <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="h-6 w-6 sm:h-7 sm:w-7 md:h-8 md:w-8">
          <path stroke="none" d="M0 0h24v24H0z" fill="none" />
          <path d="M8 21h8a5 5 0 0 0 5 -5v-3a3 3 0 0 0 -3 -3h-1v-2a5 5 0 0 0 -5 -5h-4a5 5 0 0 0 -5 5v8a5 5 0 0 0 5 5z" />
          <path d="M7 7m0 1.5a1.5 1.5 0 0 1 1.5 -1.5h3a1.5 1.5 0 0 1 1.5 1.5v0a1.5 1.5 0 0 1 -1.5 1.5h-3a1.5 1.5 0 0 1 -1.5 -1.5z" />
          <path d="M7 14m0 1.5a1.5 1.5 0 0 1 1.5 -1.5h7a1.5 1.5 0 0 1 1.5 1.5v0a1.5 1.5 0 0 1 -1.5 1.5h-7a1.5 1.5 0 0 1 -1.5 -1.5z" />
        </svg>
      </a>
    )}
  </div>
</div>

<script is:inline>
  (function setAccentRGB(){
    const hex = `${siteConfig.accentColor}`?.trim() || '#1d4ed8';
    function hexToRgb(hex) {
      const h = hex.replace('#','');
      if (h.length === 3) {
        return [
          parseInt(h[0] + h[0], 16),
          parseInt(h[1] + h[1], 16),
          parseInt(h[2] + h[2], 16)
        ];
      }
      if (h.length === 6) {
        return [
          parseInt(h.slice(0,2), 16),
          parseInt(h.slice(2,4), 16),
          parseInt(h.slice(4,6), 16)
        ];
      }
      return [29,78,216];
    }
    const rgb = hexToRgb(hex);
    document.documentElement.style.setProperty('--accent-rgb', rgb.join(','));
    document.documentElement.style.setProperty('--hero-accent-rgb', rgb.join(','));
  })();
</script>

<script is:inline>
  (function cycleNameVariants(){
    try {
      const variants = Array.from(document.querySelectorAll('.name-variant'));
      if (!variants.length) return;
      let idx = 0;
      const show = (i) => {
        variants.forEach((el, j) => el.classList.toggle('show', i === j));
      };
      show(0);
      setInterval(() => {
        idx = (idx + 1) % variants.length;
        show(idx);
      }, 2500);
    } catch(e){}
  })();
</script>

<script>
  (function(){
    const hero = document.getElementById('hero');
    if(!hero) return;
    const setFromEvent = (clientX, clientY) => {
      const rect = hero.getBoundingClientRect();
      const x = Math.max(0, Math.min(rect.width, clientX - rect.left)) / rect.width * 100;
      const y = Math.max(0, Math.min(rect.height, clientY - rect.top)) / rect.height * 100;
      hero.style.setProperty('--mouse-x', x + '%');
      hero.style.setProperty('--mouse-y', y + '%');
    };
    const onPointerMove = (e) => {
      const evt = (e.touches && e.touches[0]) || e;
      setFromEvent(evt.clientX, evt.clientY);
    };
    hero.addEventListener('pointermove', onPointerMove);
    hero.addEventListener('touchmove', onPointerMove, {passive:true});
    hero.addEventListener('pointerdown', (e) => {
      const evt = (e.touches && e.touches[0]) || e;
      setFromEvent(evt.clientX, evt.clientY);
    });
  })();
</script>
